'use client'
// Thin wrapper — implementation in Phase 4 once auth is wired up
// Returns the current session user from the NextAuth client session
export function useUser() {
  // Placeholder — will use `useSession` from next-auth/react
  return { user: null, isLoading: true }
}
